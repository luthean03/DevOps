services:
  database:
    build: ./Database
    env_file:
      - .env
    volumes:
      - ./Database/data:/var/lib/postgresql/data
    networks:
      - db-network
    restart: unless-stopped

  backend:
    build: ./Backend
    env_file:
      - .env
    depends_on:
      - database
    networks:
      - db-network
      - front-network
    restart: on-failure:3

  httpd:
    build: ./Httpd
    env_file:
      - .env
    depends_on:
      - backend
    ports:
      - "80:80"
    networks:
      - front-network
    restart: no

networks:
  db-network:
    driver: bridge
  front-network:
    driver: bridge

volumes:
  db-data:
    driver: local
